# 既知の課題・修正が必要な箇所

自分で修正していくための作業リストです。

---

## 完了済み

| 項目 | 状態 |
|------|------|
| 認証・認可（JWT + HttpOnly Cookie + ロール制御） | 完了 |
| DB移行（SQLite → Supabase PostgreSQL） | 完了 |
| 論理削除（全テーブルに deleted_at カラム） | 完了 |
| トースト通知（成功/エラーのフィードバック） | 完了 |
| パフォーマンス最適化（SWR統一、308解消、React.memo） | 完了 |
| クラウドデプロイ（Fly.io + Supabase） | 完了 |
| 月間業務管理ページ | 完了 |
| 利用者管理の改修（姓名分離、児/者区分、学年表示） | 完了 |
| Excel転記機能（Claude Vision API） | 完了 |

---

## 🟡 中優先度（UXの改善）

### 1. フォームバリデーションが不十分
- 現状：必須チェックのみ
- 修正案：`react-hook-form` + `zod` の導入
  ```bash
  npm install react-hook-form zod @hookform/resolvers
  ```

### 2. スタッフ管理ページが未実装
- 現状：`staffs` テーブルはあるがUIがない
- シードデータのスタッフ（id=1, 2）が各所でハードコードされている
- 修正案：`/staff` ページを追加、スタッフ選択UIを各フォームに追加

### 3. ページネーションが未実装
- 現状：全件取得（利用者が増えると遅くなる）
- 修正案：APIに `page` / `limit` クエリパラメータを追加

---

## 🟢 低優先度（機能追加・改善）

### 4. PDFエクスポートが未実装
- 修正案：`@react-pdf/renderer` または `jspdf` でPDF出力
- 用途：支援計画書・モニタリング報告書の印刷

### 5. 支援計画書の詳細フォームが未実装
- 現状：AI生成テキストのみ表示
- 修正案：生成したテキストを編集・保存できるフォームを追加

### 6. 通知機能が未実装
- モニタリング期日が近くなったらメール通知
- 修正案：cronジョブ + smtplib（Python標準）

### 7. AIチャットの会話管理が簡素
- 現状：ページを離れると同じsession_idが使われ続ける
- 修正案：セッション一覧画面を作り、過去の会話を選択・削除できるようにする

### 8. 支援記録の添付ファイル機能が未実装
- 修正案：`python-multipart` はすでに入っているのでバックエンドは対応可能

### 9. 統計・レポート機能が簡素
- ダッシュボードの数字だけ
- 修正案：月別グラフなど（`recharts` ライブラリが shadcn に対応）

---

## 📝 コード品質

### 10. TypeScript の型が緩い箇所がある
- `as any` や `unknown` を使っている箇所の型を厳密化

### 11. コンポーネントの分割が不十分な箇所
- `app/clients/[id]/page.tsx` が長い → タブごとにコンポーネント分割

### 12. APIのエラーレスポンスを統一
- 現状：エラーメッセージの形式がバラバラ
- 修正案：`{"detail": "エラーメッセージ"}` に統一
